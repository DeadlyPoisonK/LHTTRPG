# Compiled compendiums: built from src/packs with `npm run packs:build` (added to the release zip).
/packs/

# Foundry runtime files
*.lock

# OS junk
desktop.ini
Thumbs.db
.DS_Store

# Editor
.vscode/

# Claude Code cross-session notes (local only)
.claude/

# graphify knowledge-graph index (local dev tool cache, regenerate with /graphify)
graphify-out/

# Node dev tooling (compendium build)
node_modules/
